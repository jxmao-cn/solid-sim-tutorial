---
description: One-click bilingual group-meeting report (alias of /组会报告)
argument-hint: [optional start date YYYY-MM-DD]
---

# Group Meeting Report (alias of /组会报告)

Run the identical workflow as the Chinese command `/组会报告` — read its full instructions in `.claude/commands/组会报告.md` and follow them exactly (determine report period → collect static evidence → ask the two confirmation questions → write `reports/组会报告_<date>.md` using the fixed bilingual template → update `reports/index.md`). Never modify PROGRESS.md.
